FILE( GLOB _HEADER_FILES *.h )
FILE( GLOB _SOURCE_FILES *.cpp )

SET_PROPERTY( DIRECTORY ${MAIN_DIR} APPEND PROPERTY HEADER_FILES ${_HEADER_FILES} )
SET_PROPERTY( DIRECTORY ${MAIN_DIR} APPEND PROPERTY SOURCE_FILES ${_SOURCE_FILES} )
